# OdinEye — Development Log

## День 0 — Планування

### Ідея проєкту

Я хотів зробити легкий та гнучкий аналізатор IP для SOC-команд. Часто SOC-команди створюють власні інструменти для цього, тому я подумав, що буде добре зробити один зручний інструмент, у якому можна отримати всю потрібну інформацію про IP в деталях, налаштувати, що саме хочеш побачити, та отримати результат у вигляді PDF-файлу або таблиці, яку можна буде надати стороні, що запитала аналіз конкретного діапазону IP-адрес.

### Вибір джерел даних

Спочатку думав парсити сторінку [bgp.he.net](https://bgp.he.net/) для отримання даних про IP-адресу, але згодом вирішив, що це негарно, довше, і не надає весь спектр інформації, яку я хотів би бачити у своєму інструменті. Зупинився на тому, що краще отримувати дані за допомогою:

- протоколу **RDAP**
- запитів **reverse DNS**
- інструменту **Team Cymru IP-to-ASN**

### Чому вручну, а не Claude Code

Думав спочатку використати Claude Code, але вирішив, що краще зробити все вручну, щоб отримати більше знань. У цьому проєкті для мене головне — отримати більше знань про те, як влаштовані протоколи, набути більше досвіду з Python та розробкою структури проєкту, і в цілому покращити свої навички розробника.

Я розумію, що навички використання AI також важливі для прискорення роботи та більшої продуктивності, але вважаю, що junior-рівень, такий як у мене, повинен спочатку навчитися фундаментальним речам. І вже коли переходиш на senior-рівень з цим багажем знань — можеш використовувати AI для оптимізації та покращення продуктивності власної праці.

### Вибір фронтенду

Вирішив для фронтенду не використовувати великі фреймворки типу Svelte — це збільшить проєкт у декілька десятків разів, коли це непотрібно. Використовую **Flask** — невеликий фреймворк для вебзастосунків на Python.

### Структура проєкту
 
```
OdinEye/
├── app.py
├── requirements.txt
├── src/
│   ├── categories_maps.py
│   └── labels.py
├── templates/
│   └── index.html
└── static/
    ├── css/
    │   └── style.css
    └── js/
        ├── api.js
        ├── app.js
        ├── debug.js
        └── render.js
```
 
Така структура тому, що Flask за замовчуванням використовує папку `templates` для методу `render_template()` при генерації вебсторінки з HTML-файлу.
 
> Цитата з документації Flask:
> *"Flask will look for templates in the `templates` folder. So if your application is a module, this folder is next to that module; if it's a package, it's actually inside your package."*
 
Так само і для папки `static` — Flask рекомендує зберігати статичні файли саме там. Папка `src/` містить словники labels/categories, що мапують сирі RDAP-поля на категорії для фільтрів, а фронтенд-JS розбитий на `api.js` (мережеві запити), `render.js` (рендер DOM), `debug.js` (debug-режим) та `app.js` (оркестрація).
 
---

## День 1 — Повторення Flask

Більшу частину дня я займався повторенням web розробки та вивченням матеріалу про Flask.

Також після документацій Flask зрозумів, що його вбудований сервер не підійде для продакшену, якщо хтось буде використовувати його в масовості. Тому в план я додаю налаштування Nginx-сервера, але це вже у самому кінці.

Увесь код, що я додав у цьому коміті, — це більше тестування та згадування синтаксису.

---

## День 2 — Розбір структури RDAP

Більшу частину дня аналізував об'єкт, який я отримую з `ipwhois`. Зробив debug-функцію, яка просто аналізує IP і дає розпарсену відповідь у зручному форматі, щоб мені було легше розуміти структуру.

Експериментував, і почав записувати, що означає кожен ключ.

Завтра сподіваюсь зробити вивід у форматі таблиці без зайвих значень.

У цьому коміті можна побачити перші напрацювання.

---

## День 3-4 — Фільтри та проблема вкладеності

Розібрався як зробити фільтри, використовуючи dict, але наткнувся на нову проблему: дані можуть мати для одного поля декілька відповідей, а іноді вони вкладені один в одного декілька разів.

---

## День 5 — Tree traversal та форматування

Вирішив проблему з tree traversal у моїх ключах, за якими я парсив дані, та зробив краще форматування даних.

Наступного дня буду робити рефакторинг коду, тому що він вже виглядає складно та заплутано, та буду робити CSS для мого фронтенду.

---

## Останній день — рефакторинг, декомпозиція та CSS
 
Зробив рефакторинг коду, займався декомпозицією. Тепер рендер повністю інший, блочний — дозволяє застосовувати CSS до всіх елементів окремо. У кількох місцях виправив логіку, додав базову обробку помилок. Переробив обробку інпута — тепер він може приймати bulk IP.
 
Наостанок зробив CSS. Цю частину робив уже за допомогою Claude — дуже втомився і не хотів витрачати ще декілька днів на вивчення фронтенду та стилів.
 
Результат вийшов непоганий — виглядає вже більше як готовий прототип. Звісно, ще немає головних функцій, як CSV та PDF-експорт, і хотілося б зробити зручніший інтерфейс, але це вже в майбутніх оновленнях, якщо повертатимусь до цього проєкту.

---

## Що вивчив за цей час
 
- Отримав більше досвіду з JS — асинхронність (`async`/`await`, `fetch`), робота з масивами й об'єктами, деструктуризація.
- Розібрався, що таке рекурсія, та вирішив задачу з tree traversal для довільно вкладених структур.
- Краще став робити рефакторинг коду — виніс логіку в окремі модулі, навчився давати змінним і функціям послідовні, зрозумілі назви.
- Розібрався в темі RDAP та в тому, як працює AS, ролі Entity, вкладеність контактів.
- Попрактикувався з Flask — маршрути, `jsonify`, робота з `request`.